@model Portfolio.Models.AdminEducationListViewModel

@{
    ViewData["Title"] = "Admin - Edukacja";
    var success = (string?)TempData["Admin.Success"];
}

<div class="page-bg">
    <div class="container py-5 container-max-900">
        <div class="vs-panel">
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
                <div>
                    <h1 class="mb-1">Edukacja</h1>
                    <div class="text-muted">Zarządzanie sekcją „Wykształcenie” na stronie „O mnie”.</div>
                </div>
                <div class="d-flex gap-2">
                    <a class="btn btn-outline-dark btn-sm" asp-controller="Admin" asp-action="Index">Powrót</a>
                    <a class="btn btn-secondary btn-sm" asp-controller="Admin" asp-action="EducationCreate">Dodaj</a>
                </div>
            </div>

            @if (!string.IsNullOrWhiteSpace(success))
            {
                <div class="alert alert-success mt-4 mb-0">@success</div>
            }

            <div class="table-responsive mt-4">
                <table class="table table-sm align-middle mb-0">
                    <thead>
                    <tr>
                        <th scope="col">PL</th>
                        <th scope="col">EN</th>
                        <th scope="col">Od</th>
                        <th scope="col">Do</th>
                        <th scope="col" class="text-end">Akcje</th>
                    </tr>
                    </thead>
                    <tbody>
                    @if (Model.Items.Count == 0)
                    {
                        <tr>
                            <td colspan="5" class="text-muted py-4">Brak pozycji edukacji.</td>
                        </tr>
                    }
                    else
                    {
                        foreach (var item in Model.Items)
                        {
                            <tr>
                                <td class="fw-semibold">@item.SchoolPl</td>
                                <td>@item.SchoolEn</td>
                                <td>@item.From.ToString("yyyy-MM-dd")</td>
                                <td>@(item.To?.ToString("yyyy-MM-dd") ?? "—")</td>
                                <td class="text-end">
                                    <div class="d-inline-flex gap-2">
                                        <a class="btn btn-outline-dark btn-sm"
                                           asp-controller="Admin"
                                           asp-action="EducationEdit"
                                           asp-route-id="@item.Id">
                                            Edytuj
                                        </a>
                                        <form method="post"
                                              asp-controller="Admin"
                                              asp-action="EducationDelete"
                                              asp-route-id="@item.Id"
                                              class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit"
                                                    class="btn btn-outline-dark btn-sm"
                                                    onclick="return confirm('Usunąć tę pozycję?');">
                                                Usuń
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

