# Copy to `.env` and adjust. `.env` is ignored by git.
#
# SQL Server SA password (must meet complexity requirements):
# - min 8 chars
# - uppercase + lowercase + digit + symbol
MSSQL_SA_PASSWORD=ChangeMe_123!

# Internal admin panel (disabled by default)
ADMIN_ENABLED=false
ADMIN_PASSWORD=ChangeMe_123!

# SMTP / CV e-mail sending (OVH example)
EMAIL_ENABLED=false
EMAIL_FROM_ADDRESS=contact@tomaszkraft.pl
EMAIL_FROM_DISPLAY_NAME=Tomasz Kraft
EMAIL_HOST=ssl0.ovh.net
EMAIL_PORT=587
EMAIL_USER=contact@tomaszkraft.pl
EMAIL_PASSWORD=ChangeMe_MailPassword
